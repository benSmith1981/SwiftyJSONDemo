# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

abstract_target 'SwiftyJSONDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftyJSONDemo
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Alamofire'
  pod 'R.swift','~> 4.0.0'

target 'SwiftyJSONDemo-dev' do
    
end


target 'SwiftyJSONDemo-prod' do
    
end
  target 'SwiftyJSONDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftyJSONDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
